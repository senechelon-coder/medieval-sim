# Backgrounds

Drop a painted/illustrated background here named `main_menu_bg.png` and the
main menu will use it automatically instead of its procedural gradient
fallback (see `ui/screens/main_menu/main_menu.gd`). No code changes needed.

Recommended: portrait orientation, at least 1080x1920.
