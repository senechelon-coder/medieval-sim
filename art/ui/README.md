# UI Art

- `worldly_life_emblem_v1.png` — the title-screen logo (hourglass, globe, and astrolabe ring, with the "Worldly Life" wordmark), used by `ui/screens/main_menu/main_menu.tscn`.
- The project icon (`icon_transparent.png` at the repo root, referenced by `project.godot`) is a matching square crop for small app/taskbar sizes.
- `dropdown_arrow.png` — small gold chevron used by `OptionButton` dropdowns in the shared theme.
