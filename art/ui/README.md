# UI Art

Drop a crest/emblem image here named `crest.png` and the main menu will use
it automatically instead of its procedural vector shield fallback (see
`ui/screens/main_menu/crest_icon.gd`). No code changes needed.

Recommended: square, transparent background, at least 512x512.
